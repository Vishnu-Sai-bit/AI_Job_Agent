version: '3.8'

services:
  backend:
    build: .
    container_name: ai-job-agent-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PORT=8000
    restart: always
